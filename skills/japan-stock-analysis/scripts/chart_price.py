"""Stock-price chart (daily / weekly / monthly).

Implementation target: Phase P10 (future).

Out of scope for initial Phases. Once metrics charts are stable, this will
plug a price chart into the HTML report via Plotly.
"""

from __future__ import annotations


def render_price_chart(sec_code: str):
    raise NotImplementedError("chart_price is planned for Phase P10 (future)")
